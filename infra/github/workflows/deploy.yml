# Placeholder deployment workflow — target environment TBD.
name: deploy
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: TODO
        run: echo "Define deployment steps"
